{
  "id": "openclaw-config-generator",
  "name": "OpenClaw Config Generator",
  "description": "Third-party API configuration generator for OpenClaw. Provides a web UI to safely merge provider settings into openclaw.json.",
  "version": "1.2.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "routePath": {
        "type": "string",
        "default": "/plugins/openclaw-config-generator",
        "description": "HTTP route path to serve the config generator UI (must start with /plugins/ or /api/ to bypass Gateway SPA fallback)"
      }
    }
  },
  "uiHints": {
    "routePath": {
      "label": "Route Path",
      "placeholder": "/plugins/openclaw-config-generator"
    }
  }
}
