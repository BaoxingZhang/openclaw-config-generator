{
  "id": "config-generator",
  "name": "OpenClaw Config Generator",
  "description": "Third-party API configuration generator for OpenClaw. Provides a web UI to safely merge provider settings into openclaw.json.",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "port": {
        "type": "number",
        "default": 18800,
        "description": "Port for the config generator web server"
      },
      "host": {
        "type": "string",
        "default": "127.0.0.1",
        "description": "Host to bind the config generator web server"
      }
    }
  },
  "uiHints": {
    "port": {
      "label": "Port",
      "placeholder": "18800"
    },
    "host": {
      "label": "Host",
      "placeholder": "127.0.0.1"
    }
  }
}
